# ============================================
# 划词翻译依赖
# ============================================

# ---------- 后端 LLM ----------
openai>=1.0.0,<3.0.0           # LLM SDK（智谱API兼容OpenAI格式）

# ---------- 前端 ----------
pystray>=0.19,<1.0.0           # 系统托盘图标（依赖 Pillow，已有）

Pillow>=10.0.0,<12.0.0       # 托盘图标生成
pywin32>=306,<400             # 全局热键注册 + 剪贴板操作
